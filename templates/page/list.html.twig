{% extends 'layout/layout-two-cols-4-8.html.twig' %}

{% block regionRight %}
    <div class="block block--page-list">
        <div class="block-content">
            <div class="list list--page-list">
                {% for page in pages %}
                    <div class="list-item">
                        {{ include('@page/views/teaser.html.twig', {'item' : page}) }}
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}